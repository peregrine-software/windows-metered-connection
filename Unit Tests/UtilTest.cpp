#include "catch.hpp"
#include "Util.h"

SCENARIO("Static utils") {
    GIVEN("Invariant helpers") {
        //WHEN("I pass a wide string") {
        //    REQUIRE(MeteredConnection::Util::Narrow(LR"(C:\ProgramData)") == R"(C:\ProgramData)");
        //}
    }
}
